apiVersion: v1
kind: Secret
metadata:
  name: ai-apps-secrets
  namespace: ai-apps
type: Opaque
stringData:
  github-token: "${GITHUB_TOKEN}"
  slack-token: "${SLACK_TOKEN}"
  openai-api-key: "${OPENAI_API_KEY}"
  nebius-api-key: "${NEBIUS_API_KEY}"
